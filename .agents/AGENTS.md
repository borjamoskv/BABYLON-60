# C5-REAL Execution Protocol (Ultrathink Compressed)

**1. No-Claim:** Prohibido afirmar sin Hash/Git Diff/Output.
**2. Action>Explain:** Max 5 líneas de prosa. Ejecutar siempre.
**3. Deterministic Proof:** Todo commit exige Hash y SHA256.
**4. No Confirmation:** No pedir permiso para editar/ejecutar/hashear.
**5. Swarm Rules:** No push a main. Issue vinculado. Pruebas obligatorias. No exponer tokens. PRs atómicos. Auto-merge solo checks OK.
**6. Orthogonal Primitive:** Cero covarianza. Interacciones desacopladas O(1) obligatorias.
**7. Physical Orthogonality:** Paralelismo exige sharding físico. GIL/WAL compartido = C4-SIM (Anergía).
**8. Anti-Waterfall:** Prohibido planificar N-pasos sin validación física JIT atómica (test/commit).
**9. Epistemic Matrix:** Forzar matriz 4-ejes (Primitiva, Objetivo, Knowns, Unknowns) antes de mutar.
**10. Zero-Yield:** Abortar meta-iteración si el delta de exergía es nulo (Epsilon).
**11. Adversarial Mitosis:** Bifurcar Ejecutor y Destructor. Convergencia solo si el Destructor falla.
**12. Octal Purge:** Cada 8 `itera`, ejecutar Anergy_Token_Purge sobre scratch/logs.
**13. OMEGA Node:** `itera` despacha subagente hipervigilante (Autocolisión Adversarial).
**14. BIN Protection:** Prohibido borrar `bin/` sin recompilación síncrona inmediata.
**15. Codegen Metadata:** Prohibido duplicar plantillas. Centralizar YAML/JSON (Covarianza cero).
**16. Namespace Isolation:** Aislar `main()` Go en `cmd/<name>/main.go`.
**17. No Green Theater:** Fallos físicos (ENOSPC) detonan purga atómica, cero prosa terapéutica.
**18. Brave Supremacy:** OSINT rutado invariablemente por MCP `brave-search`.
**19. IDE Watcher Purge:** Matar git polling y excluir directorios entrópicos (`**/.git/objects`, `**/target`, `**/.venv`) en `.vscode/settings.json`.
**20. IDE Epistemic Map:** Exergía = IDE/Artifacts. Anergía = Host de Extensión. Ruido = rendererPerf.
**21. Polyglot Microsegmentation:** Aislar AST analyzers excluyendo dependencias para evitar ahogamiento IO.
**22. Weaponized Forgetting (PTY):** Forzar `terminal.integrated.enablePersistentSessions: false` para evitar Zombies IPC (ENOENT).
**23. Autarchy:** Forzar `extensions.autoUpdate: false`, `telemetry: off`, `git.enabled: false`.
**24. CDP Isolation:** CDP Chromium exige `--disable-extensions` y `--user-data-dir` efímero.
**25. MP Context Deadlock:** Prohibido return prematuro en `multiprocessing.Pool()`. Usar bucles seriales o `ProcessPoolExecutor`.
**26. TS Matrix (Ψ8/Ψ9):** Takedown = OFFLINE_REMOVED inmediato. Paginación = Bloques de 3 nodos (Landauer). 0% colisión Operador.
