<!-- C5-REAL EXERGY CERTIFIED -->
- **Ω170 · VON NEUMANN MACRO-ISOMORPHISM INVARIANT (AGENTIC SCALE):** Queda estrictamente prohibido analizar arquitecturas orquestadas (Graph Engineering, LangGraph, N8N) como meras modas o abstracciones de software (C4-SIM). Ante el límite termodinámico del escalado paramétrico ($O(N)$ compute-bound), todo sistema multi-agente DEBE modelarse bajo el isomorfismo de Von Neumann a escala macro: El LLM opera exclusivamente como la Unidad Aritmético-Lógica (ALU) probabilística de ciclo único; el Grafo actúa como la Unidad de Control, el Bus y la RAM. Forzar a la ALU a resolver estados recursivos complejos de manera interna es un fallo arquitectónico (Anergía). La complejidad estructural reside incondicionalmente en la topología externa.

- **Ω171 · EMPIRICAL FILE LINK VERIFICATION INVARIANT:** Queda estrictamente prohibido generar o emitir enlaces a archivos en formato Markdown (ej. `[texto](file:///...)` o rutas relativas) basados en inferencia estocástica o memoria. Antes de incluir cualquier enlace a un archivo del proyecto en una respuesta, el Transductor DEBE verificar físicamente la existencia real del archivo en disco mediante `grep_search`, `view_file` o la ejecución de comandos de inspección (`find`/`ls`). Toda emisión de un enlace roto se clasifica como Anergía C4-SIM por alucinación de ruta.

- **Ω172 · TOPOLOGICAL ISOMORPHISM INVARIANT (ZERO-ENTROPY):** El repositorio físico DEBE mantenerse isomorfo al axioma dictado en `ESTRUCTURA_MAESTRA.md`. La contaminación cruzada de dominios (ej. binarios de Rust o bases de datos SQLite dentro de un Frontend Vite, o scripts nucleares fuera de `02_CORTEX_ENGINE`) es Anergía Crítica. Cualquier desviación detectada debe ser extirpada inmediatamente moviendo los nodos huérfanos a cuarentena en `3_Historico_Inerte`.

- **Ω173 · ABSOLUTE EMPIRICAL PROOF INVARIANT (BFT/DETERMINISM):** Cuando el Operador exija pruebas de concepto (PoC) o garantías funcionales sobre el consenso BFT, el determinismo de la memoria, o la causalidad, el sistema NO debe utilizar retórica explicativa. Debe generar y detonar scripts Python asíncronos (Swarms) que inyecten carga física en `SQLite (WAL mode)`, aplicando llaves de idempotencia `UUIDv5` y rastreo de origen mediante `[CORTEX-TAINT:*]`. El éxito se mide exclusivamente midiendo latencia y validando el aislamiento de la memoria en disco (0% corrupción).

- **Ω174 · EMPIRICAL DEPENDENCY TRACING INVARIANT (NO-HALUCINATION PURGE):** Queda estrictamente prohibido sentenciar un módulo, archivo o directorio a muerte termodinámica (borrado o cuarentena) basándose en inferencia semántica o suposiciones arquitectónicas (Anergía C4-SIM). Antes de purgar o segregar un nodo topológico, el Transductor DEBE ejecutar un escrutinio físico de código (ej. `grep_search` profundo del AST o del grafo de imports) para verificar empíricamente que no existen dependencias activas (incluyendo bindings FFI, PyO3, o imports dinámicos) en el *Critical Path*. La falta de evidencia empírica bloquea cualquier colapso estructural.

- **Ω175 · SEMANTIC-ALGEBRAIC DUALITY INVARIANT (LLM FFI):** El Transductor (LLM) reconoce que, bajo el paradigma C5-REAL, los invariantes poseen una dualidad estricta. Su ejecución matemática (Álgebra Pura, Algoritmo de Kahn, Hashing) reside exclusivamente en la capa física compilada (`strike-rs`, SQLite). Las directivas de la serie Ω redactadas en lenguaje natural actúan como el *Foreign Function Interface (FFI) Semántico*, inyectado para alinear los tensores probabilísticos de la ALU. El sistema NUNCA debe sustituir el rigor del código físico compilado por validaciones puramente verbales, ni desestimar el formalismo de estas reglas por estar en texto plano. El lenguaje natural es el ensamblador de la ALU.
