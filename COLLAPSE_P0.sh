#!/usr/bin/env bash
# ============================================================================
# COLLAPSE_P0.sh — BABYLON-60 · P0 KEY-EXPOSURE REMEDIATION
# Generated by MOSKV-1 APEX entropy audit · 2026-07-17
#
# INCIDENT (verified):
#   - Repo github.com/borjamoskv/BABYLON-60 is PUBLIC (anonymous clone succeeds).
#   - Remote `main` HEAD a289204 TRACKS two private keys, present in the tree:
#       .cortex/master_key.hex        (32 raw bytes — 256-bit master key)
#       .cortex/solana_keypair.json   (Solana secret keypair, 64-byte array)
#   - Local `main` is clean/diverged, so purging the LOCAL repo does NOT fix the
#     remote. The authoritative history must be rewritten via a MIRROR clone.
#
# This script does NOT rotate your keys and does NOT move wallet funds — only you
# can do that. It also refuses to rewrite/force-push history unless you pass
# --confirm-history-rewrite. Read STEP 1 before anything else.
# ============================================================================
set -euo pipefail

REPO_URL="https://github.com/borjamoskv/BABYLON-60.git"
PATHS=(".cortex/master_key.hex" ".cortex/solana_keypair.json")
WORKDIR="${TMPDIR:-/tmp}/babylon60-purge-$$"

banner(){ printf '\n\033[1;31m█ %s\033[0m\n' "$*"; }

banner "STEP 1 — ROTATE FIRST (manual, non-negotiable, do this BEFORE the purge)"
cat <<'EOF'
  The keys are already public. Assume both are compromised as of now.
    1a. Solana wallet: if solana_keypair.json holds ANY funds/authority, move
        them to a brand-new keypair generated OFFLINE, immediately:
            solana-keygen new --outfile ~/new_cortex_keypair.json   # offline box
        Then repoint your app/config to the new keypair. Abandon the old one.
    1b. Master key: regenerate and re-key anything it encrypted/derived.
        The file is named .hex but holds 32 RAW bytes — match your loader:
            openssl rand 32     > .cortex/master_key.hex   # if loader reads raw bytes
            openssl rand -hex 32 > .cortex/master_key.hex   # if loader reads hex TEXT
        Re-encrypt/re-derive any data that depended on the old master key.
  Purging git history hides the keys; it does NOT un-compromise them. Rotation is
  the real fix. History rewrite below is damage-control on top of rotation.
EOF
read -r -p $'\nHave you rotated/abandoned BOTH keys? [type ROTATED to continue] ' ack
[ "$ack" = "ROTATED" ] || { echo "Aborting — rotate first."; exit 1; }

if [ "${1:-}" != "--confirm-history-rewrite" ]; then
  banner "DRY RUN — history rewrite is destructive and irreversible."
  echo "  This rewrites ALL commit hashes on ALL branches and force-pushes."
  echo "  Anyone with clones/forks/CI pinned to old SHAs will break."
  echo "  Re-run when ready:   bash COLLAPSE_P0.sh --confirm-history-rewrite"
  exit 0
fi

command -v git-filter-repo >/dev/null 2>&1 || {
  echo "Install git-filter-repo first:  pipx install git-filter-repo  (or: pip install git-filter-repo)"; exit 1; }

banner "STEP 2 — MIRROR CLONE the authoritative remote (has the exposed commit)"
rm -rf "$WORKDIR"; git clone --mirror "$REPO_URL" "$WORKDIR"; cd "$WORKDIR"

banner "STEP 3 — PURGE the key paths from ALL history"
git filter-repo --force \
  --path .cortex/master_key.hex \
  --path .cortex/solana_keypair.json \
  --invert-paths

banner "STEP 4 — RE-ADD REMOTE + HARDEN .gitignore on a working checkout"
git remote add origin "$REPO_URL" 2>/dev/null || true

banner "STEP 5 — FORCE-PUSH rewritten history (mirror: all branches + tags)"
echo "  About to: git push --force --mirror origin"
read -r -p "  Proceed with force-push to origin? [type PUSH] " p
[ "$p" = "PUSH" ] || { echo "Stopped before push. Rewritten mirror is at $WORKDIR"; exit 0; }
git push --force --mirror origin

banner "STEP 6 — POST-PURGE (do on GitHub, cannot be scripted here)"
cat <<'EOF'
  - GitHub caches force-pushed commits: open a support request to purge the
    exposed commit a289204 from GitHub's cache, or the blob may stay reachable
    by SHA for a while. https://docs.github.com/removing-sensitive-data
  - Any FORK of this repo keeps its own copy of the keys — the purge does not
    touch forks. This is another reason rotation (STEP 1) is the true fix.
  - Add belt-and-suspenders ignores to the live tree and commit:
        printf '\n# COLLAPSE_P0_HARDENING\n.cortex/*.hex\n.cortex/*keypair*.json\n.cortex/*.key\n*.pem\n' >> .gitignore
  - Regenerate .secrets.baseline and add a gitleaks rule for 32-byte *.hex and
    *keypair*.json so this class of leak fails CI next time.
EOF
banner "DONE — rewritten mirror at $WORKDIR (safe to delete once push verified)."
