[package]
name = "rust_siege"
version = "0.1.0"
edition = "2021"

[dependencies]
rusqlite = { version = "0.31", features = ["bundled"] }
chrono = "0.4"
sha2 = "0.10"
crossbeam = "0.8"
rand = "0.8"
num_cpus = "1.17.0"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
