[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
evm_version = "cancun"
optimizer = true
optimizer_runs = 200
via_ir = true
solc_version = "0.8.25"

[profile.default.fuzz]
runs = 512
seed = "0xDEADCAFE"

[profile.default.invariant]
runs = 64
depth = 20
