-- Code generated by generate_haskell.py. DO NOT EDIT.
module Main where

import Primitives.Haskell1000
import System.Exit (exitFailure, exitSuccess)
import Control.Monad (foldM)

main :: IO ()
main = do
    putStrLn "Starting Haskell Native 1000 Primitives Verification..."
    let results = foldM runPrimitive initialState [0..999]
    case results of
        Left err -> do
            putStrLn $ "❌ Verification failed: " ++ err
            exitFailure
        Right finalState -> do
            putStrLn $ "✅ Successfully verified 100% execution coverage across 1000 Haskell Primitives."
            putStrLn $ "   Execution Count: " ++ show (executionCount finalState)
            putStrLn $ "   Final Compile Cost: " ++ show (compileCost finalState)
            putStrLn $ "   Final Thunk Depth: " ++ show (thunkDepth finalState)
            putStrLn $ "   Final Monadic Depth: " ++ show (monadicDepth finalState)
            putStrLn $ "   Final Category Depth: " ++ show (categoryDepth finalState)
            putStrLn $ "   Final Concurrency: " ++ show (concurrency finalState)
            exitSuccess

runPrimitive :: HaskellStateVector -> Int -> Either String HaskellStateVector
runPrimitive vec code = do
    _ <- resolveHaskellIdentity code
    dispatchHaskell code vec
