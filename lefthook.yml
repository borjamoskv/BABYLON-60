# C5-REAL EXERGY CERTIFIED
# C5-REAL Lefthook Pre-commit Configuration (Shift-Left)
pre-commit:
  parallel: false
  commands:
    preflight:
      run: python3 scripts/c5_manifest_compiler.py && ./scripts/preflight.sh

pre-push:
  parallel: false
  commands:
    falsification:
      run: python3 scripts/verify_popper_coverage.py
