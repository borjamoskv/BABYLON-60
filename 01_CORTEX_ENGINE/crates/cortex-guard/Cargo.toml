[package]
name = "cortex-guard"
version = "0.1.0"
edition = "2021"
description = "Deterministic Architectural Verification CLI & SARIF Generator for agents.archi"
authors = ["Borja Moskv <borjamoskv@github.com>"]

[dependencies]
clap = { version = "4.5", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"
hex = "0.4"
anyhow = "1.0"
