[project]
name = "teorema-robinson-moskv"
version = "0.1.0"
description = "MOSKV-1 BFT Cortex"
readme = "README.md"
requires-python = "==3.12.*"
dependencies = [
    "chromadb>=1.5.9",
    "maturin>=1.14.1",
    "mypy>=2.3.0",
    "pytest>=8.0.0",
    "pytest-cov>=7.1.0",
    "pyyaml>=6.0.3",
    "websocket-client>=1.9.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]
