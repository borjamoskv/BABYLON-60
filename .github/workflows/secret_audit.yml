name: C5-REAL Secret Sentinel (ULTRATHINK P0)

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  schedule:
    - cron: '0 0 * * *'

jobs:
  audit-secrets:
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      contents: read
    
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        
    - name: Setup C5-REAL Python Environment
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        
    - name: Get Delta Files (Pull Request Optimization)
      id: get-deltas
      if: github.event_name == 'pull_request'
      run: |
        git diff --name-only -z origin/${{ github.base_ref }} HEAD > delta_files.txt
        
    - name: Execute Swarm Transductor (SARIF Generation)
      env:
        GITHUB_ACTIONS: "true"
      run: |
        echo "[*] Initializing BFT_STATE_LOOP Secret Auditor (ULTRATHINK)..."
        if [ "${{ github.event_name }}" == "pull_request" ]; then
          mapfile -d '' FILES < delta_files.txt
          python3 scripts/c5_quality_gates/secret_swarm_auditor.py --sarif --files "${FILES[@]}"
        else
          python3 scripts/c5_quality_gates/secret_swarm_auditor.py --sarif
        fi
        
    - name: Upload SARIF to GitHub Security Code Scanning
      uses: github/codeql-action/upload-sarif@v4
      if: always()
      with:
        sarif_file: secret_audit.sarif
        category: moskv1-apex-secrets
